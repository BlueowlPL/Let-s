<style>{%- render 'benefit-section.css',
  bg_color: section.settings.bg_color,
  font_color: section.settings.font_color,  
-%}</style>

{%- style -%}
  .benefits-list {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .benefits-list {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="benefits-list">
  {%- for block in section.blocks -%}
    <span class="benefits-list__block_text">
      {{ block.settings.title | escape }}
    </span>
  {%- endfor -%}
</div>

<div class="benefits-list benefits-list--second">
  {%- for block in section.blocks -%}
    <span class="benefits-list__block_text">
      {{ block.settings.title | escape }}
    </span>
  {%- endfor -%}
</div>

{% schema %}
  {
    "name":"Benefity",
    "tag": "section",
    "class":"section-benefits",
    "settings":[
      {
        "type": "color",
        "id": "bg_color",
        "default": "#0E0807",
        "label": "Background color"
      },
      {
        "type": "color",
        "id": "font_color",
        "default": "#FFFFFF",
        "label": "Font color"
      },
      {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 16
    }
    ],
    "blocks":[
      {
        "name":"Block",
        "type":"text",
        "settings":[
          {
              "type":"text",
              "id":"title",
              "label":"Tytuł"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "t:sections.benefits.name",
        "category": "text"
      }
    ]
  }
{% endschema %}